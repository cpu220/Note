# splice
``` markdown
* index：必需。一个整数，表示要开始改变数组的位置。如果是负数，表示从数组末尾开始计算。
* howmany：可选。一个整数，表示要从数组中删除的元素数量。如果省略或大于等于剩余的元素数量，就会删除从 index 开始到数组末尾的所有元素。如果是 0 或负数，就不会删除任何元素。
* item1, …, itemN：可选。要添加到数组中的新元素。如果不指定任何元素，splice 就只会删除元素。
* splice 方法会返回一个包含被删除元素的数组，如果没有删除任何元素，就返回一个空数组。
```